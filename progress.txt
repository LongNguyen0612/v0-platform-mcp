# Ralph Progress Log - v0-mcp Prototype Workflow

## Codebase Patterns
- v0-mcp uses TypeScript 5.4+ with strict mode enabled (ES2022, NodeNext modules)
- ES Modules throughout (type: "module" in package.json)
- Zod schemas validate all MCP tool parameters (see src/types/index.ts)
  - Pattern: Define schema → export type via z.infer
  - Location: All schemas in src/types/index.ts
- Error handling follows V0McpError pattern with categorization (see src/utils/errors.ts)
  - Pattern: ErrorHandler.categorizeError() → returns ToolResult with metadata
  - All errors include: errorType, statusCode, retryable flag
- Winston logging for structured logs (see src/utils/logger.ts)
  - Use logger.info/warn/error with metadata objects
  - Helper: logToolCall(name, success, duration)
- MCP tools registered in src/mcp/tools.ts via V0Tools class
  - Pattern: listTools() returns Tool[] with JSON Schema
  - Pattern: callTool(name, args) validates with Zod → calls service → returns result
- Tests use Jest ^30.0.2 with ts-jest ^29.4.0 (see jest.config.js)
  - Tests go in tests/unit/ mirroring src/ structure
  - Run: npm test (must pass before commit)
- Existing 4 tools: v0_generate_ui, v0_generate_from_image, v0_chat_complete, v0_setup_check
- V0 service abstraction in src/services/v0Service.ts
  - Pattern: Private request() method handles all V0 API calls
  - Pattern: Service methods return Promise<ToolResult>
  - Pattern: try/catch → this.handleError(error) for consistent error handling
- V0 API uses Platform API (POST /chats endpoint)
- Dependencies: @modelcontextprotocol/sdk ^0.5.0, openai ^4.47.1, zod ^3.22.4, winston ^3.17.0

## Project Context
- **Goal:** Implement 3-command prototype workflow
- **Commands:** prepare_prototype_context → generate_prototype → handoff_to_claude_dev
- **Core Principle:** V0 answers "What should UI look like?", Claude answers "How should system work?"
- **Branch:** feature/prototype-workflow
- **Total Stories:** 20 user stories (89 story points total)
- **Sprint 1 Target:** 28 committed points, 3 stretch points

## Sprint 1 Status
- **Started:** 2026-03-08
- **Goal:** Implement core context preparation and V0 generation
- **Committed Stories:** US-001, US-002, US-003, US-006, US-007, US-016
- **Stretch:** US-005
- **Status:** Not started

---

## Story Implementation Log

## 2026-03-08 11:42 - US-001, US-002, US-003
- **What was implemented:**
  - Created prepare_prototype_context tool for parsing natural language product descriptions
  - Implemented ContextPreparationService with intelligent text parsing
  - Extracts: product_name, goal, platform (web/mobile), screens (1-10)
  - US-002: Screen inference from UI nouns (dashboard, login, etc.) and workflows
  - US-003: Platform inference using keyword heuristics (dashboard→web, mobile app→mobile)
  - Validation logic with 3 statuses: valid, weak_input, validation_error
  - Returns confidence levels (high/medium/low) and suggestions for improvement

- **Files changed:**
  - `src/types/index.ts` - Added Zod schemas: PrototypeContext, PreparePrototypeContextSchema, PrototypeContextResult
  - `src/services/contextPreparationService.ts` - NEW service with parsing logic
  - `src/mcp/tools.ts` - Registered prepare_prototype_context tool, added handler
  - `tests/unit/services/contextPreparationService.test.ts` - NEW 31 comprehensive unit tests
  - `tests/unit/types/index.test.ts` - Added 18 schema validation tests
  - `jest.config.js` → `jest.config.cjs` - Fixed ESM configuration (module.exports)
  - `package.json` - Added NODE_OPTIONS='--experimental-vm-modules' to test scripts

- **Test Results:**
  - ✅ All 31 ContextPreparationService tests passing
  - ✅ All 18 new schema tests passing
  - ✅ Build passes (tsc)
  - ⚠️ 3 pre-existing test failures in ChatCompleteSchema (unrelated to this work)
  - ⚠️ 3 pre-existing lint errors in v0Service.ts (unrelated to this work)

- **Learnings for future iterations:**
  - **Pattern discovered:** The service returns confidence levels that help determine if more user input is needed
  - **Gotcha:** When implementing related stories (US-001, US-002, US-003), implement them together since they share the same service method
  - **Useful context:** The prepare_prototype_context tool is the entry point to the prototype workflow - it MUST return valid PrototypeContext for downstream tools
  - **Testing:** Use flexible assertions for text parsing (e.g., `toContain()` or `some()`) since exact string matching is fragile
  - **Jest ESM:** Must use NODE_OPTIONS='--experimental-vm-modules' and jest.config.cjs (not .js) for ES modules

---
## 2026-03-08 14:50 - US-006, US-007, US-008, US-009, US-010, US-011
- **What was implemented:**
  - Created generate_prototype tool for V0 API integration (step 2 of workflow)
  - Implemented PrototypeGenerationService with comprehensive V0 API call logic
  - US-006: Calls V0 Platform API with structured PrototypeContext, constructs prompt
  - US-007: Enforces "no backend" constraints in CRITICAL CONSTRAINTS section of prompt
  - US-008: Returns partial_success when not all screens detected in generated content
  - US-009: Retry logic with exponential backoff [1s, 2s, 4s], max 3 retries
    - Retries: 500, 502, 503, 429 (rate limit)
    - Does NOT retry: 400, 401, 403 (client errors)
  - US-010: Generates unique proto_<timestamp> IDs, includes preview_reference (V0 webUrl)
  - US-011: Model selection with default v0-1.5-md, logs model in metadata
  - Analyzes generated content to detect screens and extract component names

- **Files changed:**
  - `src/types/index.ts` - Added GeneratePrototypeSchema, PrototypeResultSchema
  - `src/services/prototypeGenerationService.ts` - NEW service with V0 API integration
  - `src/mcp/tools.ts` - Registered generate_prototype tool, added handler, instantiated prototypeService
  - `tests/unit/services/prototypeGenerationService.test.ts` - NEW 25 comprehensive unit tests

- **Test Results:**
  - ✅ All 25 PrototypeGenerationService tests passing
  - ✅ Build passes (tsc)
  - ⚠️ Pre-existing test failures remain (ChatCompleteSchema, V0Service - unrelated)

- **Learnings for future iterations:**
  - **Pattern:** Service returns structured result types (PrototypeResult), not ToolResult - keeps service logic separate from MCP
  - **Pattern:** Use isErrorRetryable() helper to check error messages for status codes when ErrorHandler doesn't detect them
  - **Gotcha:** When testing retry logic, use mockResolvedValue (persistent) not mockResolvedValueOnce to avoid undefined responses after retries
  - **Gotcha:** Rate limit errors (429) should be retried, not treated as client errors - check status code before rejecting
  - **V0 API:** Constructs single V0 chat with comprehensive prompt including all screens - not multiple calls per screen
  - **Prompt construction:** Use clear sections (Product, CRITICAL CONSTRAINTS, Screens, Design Style) for V0 to parse easily
  - **Screen detection:** Use flexible pattern matching (lowercase, no spaces, hyphens, underscores) to detect screen names in generated content
---
